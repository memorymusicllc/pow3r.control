# pow3r.control - Cloudflare Pages Configuration
#
# Deploy: npx wrangler pages deploy dist --project-name=pow3r-control
# Uses OAuth (no API token in env)

name = "pow3r-control"
pages_build_output_dir = "./dist"
compatibility_date = "2025-12-01"
